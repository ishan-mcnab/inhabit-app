/**
 * Placeholder for main tab routes so {@link Layout} can render tab pages
 * persistently (display toggling) while React Router still matches the URL.
 */
export function TabRouteStub() {
  return null
}
